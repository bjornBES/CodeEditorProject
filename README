# Code Editor Project

## Build

Build it using dotnet 9.0

``` bash
dotnet build -c Release
```

## Usage

Run it by doing

``` bash
dotnet run --no-build
```

if you have not build do this

``` bash
dotnet run -c Release
```

or by executing the executable

``` bash
./CodeEditor/bin/Release/CodeEditor
```
