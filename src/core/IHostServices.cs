namespace TextEditorProject.Core
{
    public interface IHostServices
    {
        // OS / platform
        // IFileSystem FileSystem { get; }
        // IClipboard Clipboard { get; }
        // IProcessLauncher ProcessLauncher { get; }

        // Time & background
        // IClock Clock { get; }
        // ITaskScheduler Background { get; }

        // Diagnostics
        // ILogger Logger { get; }

        // UI bridge (VERY thin)
        // IHostDispatcher Dispatcher { get; }
    }
}