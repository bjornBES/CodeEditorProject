namespace TextEditorProject.Core.Input
{
    public enum Key
    {
        Left,
        Right,
        Up,
        Down,

        Enter,
        Backspace,
        Delete,
        Tab,
        Escape,

        Home,
        End,
        PageUp,
        PageDown,

        A, B, C, D, // etc (optional)
    }
}
